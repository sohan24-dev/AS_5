
1️⃣ What is the difference between var, let, and const?

1. Ans => var is old way with var we create many variable same name but if your use let or const you can't create same name variable const can't be update on the other hand var and let change and update
2️⃣ What is the spread operator (...)?
2. Ans => Spread operator allways copy like you have a array  if you push any variable you use spread operator if you use you push any thing in a variable.
3️⃣ What is the difference between map(), filter(), and forEach()?
3. Ans => if you use map function you transform any element in an array and return a new array if you use filter function you use condition in array forEach function like for of function you use every elements
4️⃣ What is an arrow function?
3. Ans =>arrow function is a shorter syntax of function
5️⃣ What are template literals?
4. Ans => Template literal is a way to write srtings we use here ${} it for dynamically value change 




